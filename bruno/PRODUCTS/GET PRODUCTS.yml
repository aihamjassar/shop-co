info:
  name: GET PRODUCTS
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/products"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:5000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
