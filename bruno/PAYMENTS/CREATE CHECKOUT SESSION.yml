info:
  name: CREATE CHECKOUT SESSION
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/payments/create-checkout-session"
  body:
    type: json
    data: "{\r

      \    \"cartId\": \"68a379c06a9dba359c8087af\",\r

      \    \"couponCode\": \"\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
