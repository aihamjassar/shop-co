info:
  name: REFRESH TOKEN
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/auth/refresh-token"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
