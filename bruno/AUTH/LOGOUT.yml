info:
  name: LOGOUT
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
